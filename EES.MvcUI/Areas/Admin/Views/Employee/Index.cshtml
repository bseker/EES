

@{
    ViewBag.Title = "Employee List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        Employee
        <small>Employee List</small>

        <a href="@Url.Action("Create","Employee",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New Employee
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblEmployee" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>EmployeeId</th> 
                                 
                                <th>Name</th> 
                                 
                                <th>Surname</th> 
                                 
                                <th>DepartmentId</th> 
                                 
                                <th>SupervisorId</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblEmployee').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","Employee",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "EmployeeId" },
                     
                    { "data": "Name" },
                     
                    { "data": "Surname" },
                     
                    { "data": "DepartmentId" },
                     
                    { "data": "SupervisorId" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","Employee",new{area="Admin"})?employeeId='+row.EmployeeId+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","Employee",new{area="Admin"})?employeeId='+row.EmployeeId+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}