

@{
    ViewBag.Title = "EmployeeRole List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        EmployeeRole
        <small>EmployeeRole List</small>

        <a href="@Url.Action("Create","EmployeeRole",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New EmployeeRole
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblEmployeeRole" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>EmployeeRoleId</th> 
                                 
                                <th>RoleId</th> 
                                 
                                <th>EmployeeId</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblEmployeeRole').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","EmployeeRole",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "EmployeeRoleId" },
                     
                    { "data": "RoleId" },
                     
                    { "data": "EmployeeId" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","EmployeeRole",new{area="Admin"})?employeeRoleId='+row.EmployeeRoleId+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","EmployeeRole",new{area="Admin"})?employeeRoleId='+row.EmployeeRoleId+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}