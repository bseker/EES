

@{
    ViewBag.Title = "EvaluationComment List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        EvaluationComment
        <small>EvaluationComment List</small>

        <a href="@Url.Action("Create","EvaluationComment",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New EvaluationComment
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblEvaluationComment" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>CommentId</th> 
                                 
                                <th>CommentType</th> 
                                 
                                <th>CommentText</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblEvaluationComment').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","EvaluationComment",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "CommentId" },
                     
                    { "data": "CommentType" },
                     
                    { "data": "CommentText" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","EvaluationComment",new{area="Admin"})?commentId='+row.CommentId+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","EvaluationComment",new{area="Admin"})?commentId='+row.CommentId+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}