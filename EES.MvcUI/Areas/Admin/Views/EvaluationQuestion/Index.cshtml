

@{
    ViewBag.Title = "EvaluationQuestion List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        EvaluationQuestion
        <small>EvaluationQuestion List</small>

        <a href="@Url.Action("Create","EvaluationQuestion",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New EvaluationQuestion
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblEvaluationQuestion" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>QuestionId</th> 
                                 
                                <th>QuestionText</th> 
                                 
                                <th>QuestionWeight</th> 
                                 
                                <th>Role</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblEvaluationQuestion').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","EvaluationQuestion",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "QuestionId" },
                     
                    { "data": "QuestionText" },
                     
                    { "data": "QuestionWeight" },
                     
                    { "data": "RoleName" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","EvaluationQuestion")?questionId='+row.QuestionId+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","EvaluationQuestion")?questionId='+row.QuestionId+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}