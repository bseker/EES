

@{
    ViewBag.Title = "EvaluationQuestionList List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        EvaluationQuestionList
        <small>EvaluationQuestionList List</small>

        <a href="@Url.Action("Create","EvaluationQuestionList",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New EvaluationQuestionList
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblEvaluationQuestionList" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>Id</th> 
                                 
                                <th>QuestionId</th> 
                                 
                                <th>EvaluationId</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblEvaluationQuestionList').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","EvaluationQuestionList",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "Id" },
                     
                    { "data": "QuestionId" },
                     
                    { "data": "EvaluationId" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","EvaluationQuestionList",new{area="Admin"})?ıd='+row.Id+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","EvaluationQuestionList",new{area="Admin"})?ıd='+row.Id+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}