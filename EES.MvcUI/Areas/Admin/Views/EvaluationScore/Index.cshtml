

@{
    ViewBag.Title = "EvaluationScore List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        EvaluationScore
        <small>EvaluationScore List</small>

        <a href="@Url.Action("Create","EvaluationScore",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New EvaluationScore
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblEvaluationScore" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>ScoreId</th> 
                                 
                                <th>Score</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblEvaluationScore').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","EvaluationScore",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "ScoreId" },
                     
                    { "data": "Score" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","EvaluationScore",new{area="Admin"})?scoreId='+row.ScoreId+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","EvaluationScore",new{area="Admin"})?scoreId='+row.ScoreId+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}