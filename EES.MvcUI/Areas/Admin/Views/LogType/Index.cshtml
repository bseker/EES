

@{
    ViewBag.Title = "LogType List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        LogType
        <small>LogType List</small>

        <a href="@Url.Action("Create","LogType",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New LogType
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblLogType" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>LogTypeId</th> 
                                 
                                <th>LogTypeName</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblLogType').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","LogType",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "LogTypeId" },
                     
                    { "data": "LogTypeName" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","LogType",new{area="Admin"})?logTypeId='+row.LogTypeId+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","LogType",new{area="Admin"})?logTypeId='+row.LogTypeId+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}