

@{
    ViewBag.Title = "SystemLog List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        SystemLog
        <small>SystemLog List</small>

        <a href="@Url.Action("Create","SystemLog",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New SystemLog
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblSystemLog" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>LogId</th> 
                                 
                                <th>UserId</th> 
                                 
                                <th>DateTime</th> 
                                 
                                <th>LogTypeId</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblSystemLog').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","SystemLog",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "LogId" },
                     
                    { "data": "UserId" },
                     
                    { "data": "DateTime" },
                     
                    { "data": "LogTypeId" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","SystemLog",new{area="Admin"})?logId='+row.LogId+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","SystemLog",new{area="Admin"})?logId='+row.LogId+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}