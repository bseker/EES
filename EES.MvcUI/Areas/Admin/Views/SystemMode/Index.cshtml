

@{
    ViewBag.Title = "SystemMode List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        SystemMode
        <small>SystemMode List</small>

        <a href="@Url.Action("Create","SystemMode",new{area="Admin"})" class="btn btn-xs btn-primary pull-right">
            <i class="glyphicon glyphicon-plus"></i> Add New SystemMode
        </a>
    </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                @*<div class="box-header">
                        <h3 class="box-title">Data Table With Full Features</h3>
                    </div>*@
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="tblSystemMode" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                 
                                <th>ModeId</th> 
                                 
                                <th>ModeName</th> 
                                 
                                <th>IsCurrentSystemMode</th> 
                                 
                                <th data-orderable="false"></th>
                            </tr>
                        </thead>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
</section>

@section scripts{
    <script>
        $(function () {
            $('#tblSystemMode').DataTable({
                "processing": true,
                "ajax": '@Url.Action("List","SystemMode",new{area="Admin"})',
                "deferRender": true,
                "columns": [
                     
                    { "data": "ModeId" },
                     
                    { "data": "ModeName" },
                     
                    { "data": "IsCurrentSystemMode" },
                     
                    {
                        "class": "text-center",
                        render: function (data, type, row) {
                            return '<a class="btn btn-xs btn-info btn-flat" href="@Url.Action("Edit","SystemMode",new{area="Admin"})?modeId='+row.ModeId+'">Edit</a> ' +
                                   '<a class="btn btn-xs btn-danger btn-flat" href="@Url.Action("Delete","SystemMode",new{area="Admin"})?modeId='+row.ModeId+'">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>
}